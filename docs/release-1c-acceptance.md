# Release 1C Violet Sight 验收

> 状态：Release 1C/1C.1 均为实现候选，未正式交付。手动 Context、视觉矩阵 20/50、
> 普通非视觉问答和 AX 快速通道已通过；单显示器截图和模型定位修复已完成代码实现，
> 尚待部署和真实复测。完整门槛通过前不得合并 MR !20 或进入 Release 1D。变更过程见
> [历史记录](./历史记录.md)。

## 1. 当前基线

```bash
pnpm check
pnpm macos:test
VIOLET_SWIFTPM_DISABLE_SANDBOX=1 pnpm macos:app
```

- TypeScript/JavaScript：107 项通过。
- Swift：63 项通过。
- Mac App：资源打包、ad-hoc 签名和深度签名校验通过。
- 分支：`feat/1c1-natural-pointing`；本地候选尚未推送。
- 运行代码：提交 `88a0869`；Devbox Core 健康，Mac 单进程在线。
- 账本：`805 / 1..805`，连续。
- 关键修复：`d3fbd55`（UUID/时钟）和 `2753351`（图片 WebSocket 容量）。

## 2. 当前候选

- `Look` 默认关闭；普通问题不截图。
- Qwen 判断需要视觉后，Core 才按当前 `turnId` 请求 Mac 采集。
- Mac 在 `input.speech.stopped` 到达时按 `turnId` 固定前台目标、AX 焦点和鼠标坐标。
  后续视觉请求必须在 30 秒内消费同一轮锚点；AX 失败后才截取鼠标所在的单个显示器。
- 图片出境前执行本地 OCR、绝对秘密阻断和受控敏感遮挡；OCR 原文不作为目标证据，
  OCR 识别置信度也不限制截图本身的证据置信度。
- 图片在 `8 MiB` 内保持原始像素尺寸；超限时先降低 JPEG 质量，仍超限才等比缩小。
- DeepSeek 接收原图、鼠标点和用户原问题，只调用一次，返回答案、置信度、目标类型、
  边界框和必要的文本。
- Core 校验请求关联、新鲜度、置信度、位置、颜色和目标语义；图片缺少鼠标点、目标框
  未包含该点、定位环被当作目标，或选区缺少完整文字时拒绝回答。
- 校验失败时明确返回不可可靠定位，不使用旧 Context 或模型猜测兜底。
- 新语音轮次取消旧视觉任务；截图不进入长期账本或记忆。

当前未实现且不属于本方案：拖拽监听、剪贴板读取、Trae 专用适配器、持续采帧和图片
持久缓存。

## 3. 已通过证据

| 能力 | 结果 |
|---|---|
| DeepSeek Vision 合成 canary | 两张相反方向图均正确识别文字、颜色、位置和箭头方向 |
| 手动 `Selected Text` | Trae/Electron 编辑器可建立并使用精确文字 Context |
| 手动 `Window or Display` | 可选择窗口/显示器并基于画面回答 |
| 手动 `Region` | 可框选局部区域；Y 轴坐标修复后内容与回答一致 |
| Region/浏览器页面矩阵 | 10/10 正确，坐标准确率 100% |
| 桌面文件或图标矩阵 | 10/10 正确，坐标准确率 100% |
| 普通非视觉问题 | `Look` 开启时 Context 指标不增加，正常直接回答 |
| AX 按需视觉 | 真实 Qwen 工具路由、Mac AX 捕获和 Qwen 播报闭环通过 |
| 连续三轮新鲜度 | 同一会话三次不同 AX 选区均正确，Context 指标恰好增加三次 |
| 模型调用 | 自动测试确认小目标也只使用完整原图调用一次 |

已确认的稳定性修复：

- Swift 大写 UUID 与 Core 小写请求索引改为大小写无关匹配。
- Mac 与 Devbox 约 49ms 时钟偏差不再误拒新证据；允许 30 秒跨设备偏差，仍校验请求
  过期和 `turnId`。
- 真实图片 Context 约 450–540 KB，原 150 KB WebSocket 上限会触发 close code 1009。
  当前上限为 12 MiB，并由“大图片事件后同一连接继续回答”的集成测试保护。

## 4. 已知失败

### Trae 终端精确选区

- Trae 集成终端不暴露 `AXSelectedText`，只能退化为截图。
- 旧候选将 `2940 x 1846` 图像错误变为 `2048 x 1846`，产生白区并导致定位环错位；
  DeepSeek 又把绿色 diff 或定位环当成目标。
- 本地候选已改为单显示器原尺寸截图、超限等比缩放、意图驱动定位和单次模型调用。
- Core 继续失败关闭；终端真实场景尚未复测，因此本项仍记录为失败。

### 其他未通过项

- 旧候选曾把 Source Control `33` 徽标识别为行号。
- 旧候选曾把右下角绿色发送按钮识别为白色“全部确认”。
- 唤醒真实短门禁为 15/20，低于 19/20；不得继续降低 threshold。

## 5. 剩余门禁

### Natural Pointing

1. `Look` 关闭时 20 次唤醒产生的屏幕或 AX Context 为 0。
2. `Look` 开启但问题不依赖视觉时，20 次均不截图、不上传。
3. AX 可读时不截图；AX 不可读时才进入截图链路。
4. 连续变化的窗口或鼠标问题只能使用当前 `turnId` 证据。
5. 鼠标指向、`33` 徽标和绿色发送按钮必须正确识别。
6. 低置信度、无目标框、位置或颜色冲突时必须明确失败。
7. 选中文字、鼠标附近文字、文章、图片和图表各 10 次，正确率至少 90%，跨轮误命中为
   0。

### 视觉、隐私与生命周期

- 视觉矩阵已完成 20/50；图片预览、PDF 和 IDE 各 10 次待测。
- 保密应用和绝对秘密出境数量为 0；受控敏感遮挡召回率至少 95%。
- 原始截图、敏感 OCR 原文和临时明文不得进入日志、账本或长期记忆。
- 关闭、替换、超时、锁屏、睡眠、撤权和退出共 50 次，过期 Context 接受数量为 0。
- 删除或替换 Context 必须取消后台 Vision，旧任务不得回写。

### 唤醒

- 新候选先重新执行 20 次正常距离门禁，至少 19/20 且无误触发。
- 通过后再执行 100 次正样本、1000 条负样本和 8 小时办公环境验证。
- 锁屏、睡眠、切换用户和 Realtime 会话期间 KWS 采集为 0。
- 唤醒前 PCM 写盘、上传和日志记录数量为 0。

## 6. 数据边界

- Context 默认 5 分钟过期；关闭浮层、锁屏、睡眠、撤权或会话结束时清理。
- 临时图片使用独立 DEK 加密后写入 `violet/tmp/context/`，会话结束主动删除全部版本和
  delete marker；TOS 生命周期 24 小时兜底。
- Context 不进入长期记忆，也不改写现有对话账本。
- 验收日志只能记录时序、类型、长度、状态和随机 ID，不记录音频、转写、回复或截图。

## 7. 回滚

- 关闭 `Look`：停止自动按需感知，保留手动 Context 和普通语音。
- `VIOLET_VISION_PROVIDER=deterministic`：停止真实视觉模型调用。
- `VIOLET_CONTEXT_STORAGE_PROVIDER=memory`：停止 TOS 临时对象写入。
- 删除 Context Session：Release 1B 文字与实时语音路径保持不变。
- Vision 超时、路由失败或证据校验失败：明确失败，不使用旧 Context 替代。
