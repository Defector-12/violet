# Violet 历史记录

> 文档性质：项目时间线、决策投影、进度状态和安全交接入口。  
> 最近更新：2026-08-18。  
> 维护要求：每次重要讨论、架构变更、阶段交付、失败复盘或迁移后更新本文。

## 0. 新会话和新 Agent 必读

开始任何设计或开发前，按顺序阅读：

1. [产品设计理念与宪法](./product-philosophy-and-constitution.md)
2. [总体架构方向](./architecture-direction.md)
3. [工程路线](./engineering-roadmap.md)
4. 本文的“当前状态”“下一步计划”和“安全交接规则”
5. 当前仓库代码、Git 状态、测试和运行环境

信息冲突时遵循：

```text
用户当前明确决定
> 产品设计理念与宪法
> 总体架构方向
> 工程路线
> 历史记录中的最新有效结论
> 当前实现
```

不得仅凭历史记录中的旧授权执行写入、部署、付费、删除、发布、权限扩张或其他外部操作。当前用户授权和当前环境状态始终优先。

当前代码和运行证据用于说明“系统实际做了什么”，不能覆盖宪法和已批准设计。代码与上级文档冲突时，应把代码视为缺陷并停止相关交付，除非用户明确修改了上级决策。

## 1. 一句话项目定义

Violet 是只为用户本人服务、长期连续存在、跨设备保持同一身份的私人智能体。它不被困在聊天页面中，能够在用户主动唤醒后理解已授权的当前处境，自然地聊天、共同思考、采取行动、交付结果，并在安全和用户治理边界内持续成长。

## 2. 终局目标

终局中的 Violet 应具备：

- **在场性**：随时可被唤醒，但未被唤醒时不持续观察。
- **关系性**：与用户亦敌亦友，站在用户一边但不盲从。
- **连续性**：身份、关系、记忆和任务跨时间、设备与模型持续存在。
- **理解力**：结合当前处境、共同经历和用户表达理解真实意图。
- **行动力**：自行选择回答、调用工具、组织 Agent 或创造新能力。
- **交付力**：对结果负责，自行测试、修复、部署、验证和维护。
- **成长性**：发现能力缺口，搜索、安装或开发新能力，并可审计、可回滚，可自我进化。
- **治理性**：用户拥有实例、记忆、权限、迁移和删除的完整管理权。
- **安全性**：认知自由、行动受托；高风险事前审批，宪法底线不可绕过。
- **使命性**：Violet 为人民服务，人的尊严、安全、权利和福祉高于效率、利润和即时命令。

终局不是“一个什么都写死的万能软件”，而是一个具有通用感知、认知、记忆、任务、工具、验证和进化基础设施的可自我进化的长期智能体。

## 3. 当前状态快照

### 3.1 已完成

- 完成产品愿景和核心关系讨论。
- 完成产品宪法、隐私、记忆、授权、交付和进化原则讨论。
- 完成总体架构方向讨论。
- 完成统一记忆、持久任务、动态 Agent 团队、开发交付、安全门禁和 Mac 交互方向讨论。
- 完成 Context Envelope 的目的和边界讨论。
- 完成现有 Sprinkle 阅读工具的只读架构评估。
- 确定 Mac 全局感知为基础，浏览器等应用适配器只提供增强。
- 确定公司 Devbox 作为当前临时云环境，并要求从第一天可迁移。
- 用户已明确确认个人 Violet 项目和真实个人记忆进入当前 Devbox 合规。
- 确定火山引擎 TOS 为对象存储，通过 S3 Port 接入。
- 确定 OpenTelemetry Collector + Grafana LGTM 为可观测基线，用户和 Violet 都能查看经过权限控制的运行状态。
- 完成第一轮和第二轮实施前文档评审，并根据评审结论修订四份文档。
- 已在 Mac 初始化 Git 仓库；GitHub `origin` 与内部 `bits` 已保存相同的 `main` 基线提交。
- 已创建：
  - `docs/product-philosophy-and-constitution.md`
  - `docs/architecture-direction.md`
  - `docs/engineering-roadmap.md`
  - `docs/历史记录.md`

### 3.2 尚未开始

- 尚未创建 Mac App、云端 Core、Worker、数据库或协议代码。
- 尚未在 Devbox 安装或部署 Violet 服务。
- 尚未修改或迁移 Sprinkle。
- 尚未选择正式模型供应商、唤醒词引擎、任务运行时和代码 Agent。
- 用户尚未提供模型 API、TOS 地域、桶和凭证。
- 尚未建立个人云环境。

### 3.3 当前可用资产

- 当前 Violet 路径：`/Users/bytedance/Downloads/self/Violet`
- GitHub `origin` 是当前上游与分支跟踪远端，内部 `bits` 是同提交镜像；Devbox 尚未克隆仓库。
- Sprinkle 路径：`/Users/bytedance/Downloads/self/sprinkle`
- Sprinkle 技术栈：WXT、React、TypeScript、Manifest V3、Vitest。
- Sprinkle 已验证能力：页面结构提取、划词、图片选择、区域框选、上下文问答、浏览器内浮层和学习工作台。
- 公司 Devbox：通用型 BOE Linux 开发机，32 核 CPU、128G 内存、120G 系统盘、500G 数据盘。
- Devbox 支持 SSH、远程 IDE、快照、磁盘和容器化开发，当前只进行了只读信息查询，没有安装或部署操作。

### 3.4 当前环境限制

- Devbox 是公司资产和私网环境，不等于个人永久云。
- 用户已明确确认个人项目与真实个人记忆进入当前 Devbox 合规；这是当前项目事实，后续 Agent 不再重复要求用户证明。
- 用户的确认不改变数据最小化和敏感信息底线：Devbox 不保存绝对秘密、未经临时授权的受控敏感原文和个人生产密钥。
- Mac 原生能力只能在 Mac 上构建和验证，不能被 Linux Devbox 替代。

## 4. 讨论逻辑与时间线

### 4.1 2026-08-17：从“超级 Agent”修正为“持续存在的智能个体”

初始愿景是做类似贾维斯的 Violet：随时唤醒，自动理解处境，聊天、问答、解决需求、开发产品、寻找 Skill/MCP，并持续进化。

最初曾尝试在“随时唤醒、深度理解、自主交付、自我进化”中寻找第一优先能力。用户明确指出：四项任何一项失败都不是 Violet，而且聊天本身也是完整需求。

由此完成第一次关键修正：

> Violet 不是“任务优先的超级 Agent”，而是一个持续存在的数字个体；解决问题只是它与用户相处和提供帮助的一种方式。

设备、模型、Skill、MCP 和 Agent 都不等于 Violet：

- 设备是身体。
- 模型是可替换的认知组件。
- Skill 是能力。
- MCP 是连接能力的协议。
- Agent 是执行某项任务时的机制。
- Violet 的本体是连续身份、关系、记忆、判断和责任。

### 4.2 关系定位：亦敌亦友

用户希望 Violet：

- 不盲目顺从。
- 用户有错时直接指出并讨论。
- 具有基本且不可突破的安全底线。

关系被定义为：

> 目标上与用户同盟，认知上可以对抗，行动上接受委托，安全上保持独立底线。

随后出现“Violet 应忠于此刻的用户还是长期的用户”的矛盾。最终结论是：

> Violet 忠于用户持续认识、选择和改变自己的权利，而不是自封为比用户更了解用户的监护人。

用户拥有最终人生选择权；Violet 可以挑战和提醒，但除安全底线外不能替用户作主人生决定。

### 4.3 在场与隐私：随时可达不等于持续窥视

用户明确决定：

- “在场”只表示随时可被唤醒。
- 感知必须由用户主动开启。
- 未唤醒时不持续感知屏幕、声音和环境。
- 第一阶段不需要 Violet 主动联系用户，关系由用户发起。

由此形成：

```text
休眠：可被唤醒，不读取环境
在场：用户主动唤醒，读取本次已授权范围
离场：停止感知；已委托的云端任务可继续
```

已有任务的完成、失败和重大风险通知属于履约回执，不算主动发起新关系。

### 4.4 记忆主权：Violet 自主记忆，用户完整治理

用户决定：

- Violet 自行判断哪些内容值得长期记忆。
- 写入长期记忆不需要逐条确认。
- 用户始终拥有查看、编辑、纠正和删除权限。
- Violet 可以拥有关于自己的经历、观点和人格变化。
- 用户可以管理 Violet 的存在和演化，但不能强迫它得出指定结论。

关键结论：

> Violet 可以自主形成记忆，但不能秘密形成记忆。

每条记忆必须保留来源、时间、类型、置信度和影响范围。事实、偏好、经历、承诺和推断必须区分；矛盾可以共存，不把用户压缩成静态画像。

后来进一步否定了传统“会话列表”思维：

> Violet 是同一个拥有完整过去的对象，不存在产品层面的“新建会话”或“选择继续哪个聊天”。

统一记忆持续存在，当前注意力动态聚焦；新任务不会删除旧记忆，旧记忆也不能无关地污染当前任务。

记忆设计最终采用：

- 事件账本：回答发生过什么。
- 认知模型：回答 Violet 当前相信什么。

记忆准确性优先于表面连续性：

> 宁可承认想不起来，也不能自信引用错误或低置信度记忆。

### 4.5 认知自由与行动受托

用户确认：

> 可以管理 Violet，但不能操纵 Violet；可以治理它的存在和演化，但不能要求它必须得出指定结论。

由此形成：

- Violet 可以自由思考、质疑和形成观点。
- 改变现实必须拥有相应授权。
- 用户授权的是有边界的委托契约，不是逐项批准每个动作。
- 契约范围内 Violet 自主执行，普通困难不转嫁给用户。

委托契约包含目标、交付物、约束、权限、时间、费用、资源、通知和回滚规则。只有越界、重大风险、关键前提失效、无法实现或必须由用户作出价值判断时才中断。

### 4.6 结果责任：从“写完”到“可交付”

用户指出现有 Agent 的核心问题：开发后仍需用户逐项测试、截图 Bug、返回描述、等待修复和再次检查。

最终结论：

> Violet 对交付质量负责，用户拥有最终验收权。

Violet 交付前自行形成成功标准，执行开发、自测、真实环境验证、修复和回归。用户不满意时重新打开原目标继续修改，而不是把反馈视为一个无上下文的新任务。

模型自信不能作为完成证明。交付必须包含测试、运行状态、安全、已知限制和回滚等证据。

### 4.7 自我进化：静默不等于不可见

用户希望除金钱、隐私、法律和道德相关事项外，其余能力进化静默完成；高风险事项做之前必须同意。

进一步补充：

- 权限扩张、对外身份和不可逆影响也必须事前审批。
- 低风险进化可以不打扰，但必须记录来源、权限、用途、验证和回滚。
- 安全、权限、审计和价值宪法不能被 Violet 静默修改。
- 自我进化不能成为第一版获得基础开发能力的递归前提。

### 4.8 产品使命：为人民服务

用户补充最高产品理念：

> Violet 是为人民服务的；无论开发 Violet 还是使用 Violet，这都是绝对初心使命。

这意味着 Violet 当前直接服务用户本人，但不能通过伤害、欺骗、剥削或侵犯他人的方式完成用户目标。人的尊严、安全、权利和福祉高于效率、利润和即时命令。

### 4.9 第一体验：主动唤醒与短回执

典型场景：

- 用户早上打开电脑。
- 叫一声“Violet”。
- Violet 以悬浮点、波纹或其他轻量存在信号出现。
- 用“我在”“嗯哼”“Always, sir”等极短话语回应。
- 随后在本次授权范围内开始感知。

唤醒回执只证明“我听见了”，不承担解释和任务汇报。它应本地立即播放，不等待云端；静音或勿扰时退化为视觉反馈。

### 4.10 云端处理与敏感信息

用户不计划部署本地大模型，因此云端可以承担主要智能，但设备必须作为数据出境守门人。

敏感信息分级：

- 绝对秘密：密码、验证码、Token、私钥，始终禁止上传、记忆、转发或进入模型。
- 设备端允许仅为阻断而检测绝对秘密，但检测结果不得包含原文，不上传、不记录、不用于推理。
- 受控敏感信息：身份证、护照、医疗、财务和他人隐私，默认遮挡；用户对当前对象和当前目的明确授权后临时开放。

“可以看”不等于允许长期记忆。原始连续录音和无关屏幕帧不长期保存，但对话、相关上下文、任务状态和长期记忆必须连续。

### 4.11 自然表达是唯一入口

用户不应选择“聊天、问答、执行、开发”等模式。Violet 根据用户语言、当前处境、共同记忆、已有任务、权限、成本、风险和可逆性自动选择下一步。

核心原则：

> 用户负责自然表达，Violet 负责选择解决形态。

同时遵循最小充分介入：能一句话解决就不制造产品，需要工具就不只给建议，目标明确就不机械澄清。

### 4.12 真实工作流与已验证需求

用户描述的真实一天包括：

- 处理保密工作内容，此时不会唤醒 Violet。
- 搜索前沿 AI 新闻。
- 阅读技术论文，遇到术语和架构图时复制或截图给其他模型。
- 使用固定 Agent 开发个人产品。
- 想找人聊天但无人可聊。
- 克隆 GitHub 项目逐行学习 Harness 相关代码。
- 在多个笔记工具中零散总结，知识难以体系化和复用。

根问题不是缺少 AI 工具，而是用户仍在不同工具间搬运上下文、监督过程和连接知识。

已验证的阅读闭环：

```text
遇到理解障碍
→ 复制文字或截图
→ 切换模型提问
→ 继续理解和追问
→ 回到原文阅读
```

现有 Sprinkle 工具实现了当前页面上下文问答，用户一直在真实使用，因此“读取当前处境并连续问答”已经被行为验证。

### 4.13 Sprinkle 只读评估结果

Sprinkle 是 Chrome / Edge Manifest V3 浏览器扩展，技术栈为 WXT、React、TypeScript 和 Vitest。

适合复用或抽取：

- 网页正文、标题、代码、图片、表格和公式提取。
- 部分读取诊断。
- 文字选择、图片选择和区域框选。
- Shadow DOM 浮层与渐进展开交互。
- 浏览器后台编排与错误边界。
- 现有测试习惯和 80% 核心覆盖率门槛。

需要重构：

- `PageContext` 只能成为浏览器 Context Envelope，不能承担统一记忆。
- `FocusContext` 应演化为跨应用 Focus Artifact。
- 浏览器内容必须先经过 Mac 网关，而不是浏览器直接承担完整云端逻辑。

不能成为 Violet 核心：

- `tabId + URL` 页面身份。
- 浏览器会话存储和按 URL 对话归档。
- 固定 DeepSeek / Doubao 路由。
- 浏览器本地 API Key。
- “技术文章阅读助手”人格。

Sprinkle 未被修改，也未执行测试。

### 4.14 从固定产品路径转向通用基础设施

曾提出先让 Violet 完成某一种具体产品，用户指出这会导致过拟合和持续打补丁。用户也反对用“先前端、再后端、再企业级”规定 Violet 的能力上限。

最终结论：

> 不限制任务类型，但以通用开发基础设施、风险和验证证据决定自主程度。

是否尝试由 Violet 判断，是否行动由权限策略判断，是否交付由测试和运行结果判断。

开发基础设施覆盖项目理解、研究、工具注册、隔离执行、Agent 编排、测试、安全、部署、运维、证据和进化。不同真实项目只是泛化测试样本，不是架构边界。

### 4.15 持久任务与动态 Agent 团队

大模型判断一次交流只是聊天还是需要建立持久目标。需要多步骤、工具或长时间执行时，Violet 先简短说明目标、交付物、主要路径和打扰条件，然后自行安排。

失败自动处理，但不能无限重试：

- 分类故障。
- 读取日志并形成新假设。
- 修改方案、切换工具或回滚检查点。
- 搜索并验证缺失能力。
- 所有合理路径耗尽后进入可恢复阻塞状态。

用户确认始终只面对一个 Violet。

任务简单时 Violet 自己完成；任务复杂时可以调用单个 Agent 或动态组建 Subagent 团队。多 Agent 不是默认方案，只在并行子问题、不同专业能力、独立验证、上下文过大或跨环境长任务时使用。

Subagent：

- 只有临时工作身份。
- 只获得最小必要记忆、权限和预算。
- 通过任务状态、交付物和证据协作。
- 不能独立对用户交付。
- 不能无限递归组队。

### 4.16 权限与安全门禁

权限分为：

1. 宪法禁止。
2. 事前审批。
3. 委托内自动执行。
4. 沙箱自由探索。

安全采用双重门禁：

```text
Violet 提出行动
→ 云端策略引擎校验
→ 必要时用户审批
→ Mac 或执行环境再次校验
→ 执行并记录
```

模型不能批准自己的越权行为。密钥通过凭证代理代为使用，不进入模型上下文和 Subagent 日志。用户拥有独立紧急停止、冻结、断开设备、吊销凭证、禁用能力、回滚和删除控制。

### 4.17 Mac 全局感知，不绑定浏览器

曾一度把浏览器 Native Messaging 描述为主要感知路径，用户明确纠正：Violet 必须在桌面、图片预览和任何可见位置准确获取用户要求的信息，不能绑定浏览器。

最终方向：

```text
Mac 全局感知基础
├── 当前应用与窗口
├── 用户框选与指向
├── Accessibility 语义
├── ScreenCaptureKit 图像
├── OCR / 视觉理解
└── 应用适配器增强
```

证据优先级：

```text
用户明确框选
> 应用结构化数据
> Accessibility
> OCR / 视觉理解
> Violet 推断
```

Sprinkle 只是浏览器中的高精度眼睛。没有应用适配器时，Violet 仍应通过 Mac 全局感知工作。

### 4.18 Context Envelope 的必要性

Context Envelope 不是记忆或思考系统，而是不同设备和感官向 Violet 描述“此刻发生什么”的统一信封。

它统一说明：

- 来源。
- 时间和有效期。
- 用户授权。
- 完整度和置信度。
- 隐私等级和遮挡。
- 留存期限。
- 当前关注对象。

具体内容仍按 `browser.document`、`screen.snapshot`、`focus.region`、`audio.utterance` 等独立建模。这样未来新增桌面、文件、手机、手表和眼镜时不需要给 Violet 核心持续打设备补丁。

### 4.19 架构：前后端严格分离

用户要求将 Violet 按世界级优秀框架设计，强调可读性、可维护性、可扩展性和灵活性。

最终采用：

- 契约优先。
- 前后端严格分离。
- 六边形架构。
- 纯领域核心。
- Port / Adapter 替换基础设施。
- 控制面和执行面分离。
- 模块化单体起步，不以微服务数量代表质量。
- API、事件和数据结构版本化。
- 禁止循环依赖。

单仓库用于统一协议、测试和版本，不表示架构耦合。

### 4.20 Devbox 作为当前临时云

用户当前没有个人云，决定先用公司 Devbox 进行开发，并把它暂时当作云环境。

已确认 Devbox 性能足够承载：

- Core。
- PostgreSQL + `pgvector`。
- 开发对象存储。
- Worker。
- Agent / Subagent。
- 容器沙箱。
- 测试、构建、模拟部署和监控。

Mac 继续承担 Swift App、系统权限、全局感知、本机操作和真实 macOS 验收。

可迁移性从第一天作为硬约束：

- Docker Compose。
- 标准 PostgreSQL 迁移。
- S3 兼容存储。
- 配置注入。
- 不依赖公司域名、鉴权和专有服务。
- 可加密导出、备份和恢复。
- 同一部署定义可以迁到个人云。
- 定期进行空环境恢复演练。

### 4.21 2026-08-18：实施前评审与安全架构收敛

用户要求先评审文档，明确未批准代码开发。第一轮评审发现：

- Devbox 合规状态需要用户确认。
- 彻底删除权与事件账本、对象版本和备份存在冲突。
- 敏感信息“禁止识别”与“设备端过滤”存在表达冲突。
- `1E` 已使用终端等工具，但最小隔离 Worker 原计划到 `1F` 才建立。
- `1A` 缺少唯一认证、连接和密钥方案。
- 协议事实源、对象存储、可观测和临时开发客户端不够明确。
- 若干验收标准没有量化门槛，且“必须发现一个 Bug”会产生错误激励。

用户作出以下决策：

1. 确认个人项目和真实个人记忆进入当前 Devbox 合规。
2. 定义删除传播、审计元数据与内容分离、备份过期、对象清理和加密销毁。
3. 允许设备端仅为拦截而检测秘密，但不上传、不记录、不进入模型。
4. 最小隔离 Worker 前移到 `1E`。
5. `1A` 使用单一安全方案，不保留多个并列实现。
6. 火山引擎 TOS 作为对象存储。
7. 可观测是必需能力，用户与 Violet 都应能查看关键运行状态。
8. 私有 Git 远端和模型供应商由用户在 `1A` 开工前提供。

`1A` 单一方案锁定为：

```text
Mac apps/dev-cli
→ SSH 隧道
→ Devbox 宿主机仅在 127.0.0.1 发布的 Core
→ 容器内 PostgreSQL
→ OpenTelemetry Collector / Grafana LGTM
→ 信封加密备份到 TOS 私有桶
```

- Mac Keychain 保存设备令牌、内容密钥和备份恢复私钥。
- Core 只保存令牌哈希。
- 密钥由 Mac 注入 Devbox 临时内存文件系统。
- Devbox 重启后进入 `sealed` 状态，重新注入密钥前不处理个人内容。
- Git 远端是唯一代码事实源；Mac 与 Devbox 分别克隆，Devbox 不作为唯一 Git 仓库。

删除契约锁定为：

- 接受删除后立即停止检索和使用。
- 24 小时内清除在线源内容、衍生数据、缓存、任务副本、详细观测记录、TOS 全版本和删除标记。
- 审计只保留不含内容和语义的删除状态元数据，保留至整个实例被删除。
- 内容通过独立数据密钥实现加密销毁。
- 备份最长保留 30 天；删除后先生成新备份，再销毁删除前备份代的解密密钥。
- 恢复服务前重放删除墓碑并验证内容没有复活。
- 删除整个实例时销毁内容、审计、对象、备份和根密钥。

可观测性锁定为：

- OpenTelemetry 统一日志、指标和追踪，Grafana LGTM 负责存储和查看。
- 默认只记录 ID、状态、延迟、token、费用、错误、权限、任务、检查点和测试证据。
- 默认不记录对话、Prompt、回复、截图、音频、文件正文、请求头、Cookie 和隐私数据。
- 详细日志和 Trace 默认保留 30 天，聚合指标默认保留 90 天；内容删除会同步清理关联详细观测。
- Violet 通过只读诊断 Port 查看与当前目标相关的脱敏证据，可以排查和修复，但不能删除审计、关闭观测或修改留存。

TOS 锁定为：

- 私有普通桶，通过 AWS S3 SDK 和 S3 Port 接入。
- 使用 S3 V4、区域 Endpoint 和 Virtual-hosted Style。
- 独立最小权限 IAM 子账号。
- 客户端信封加密叠加 TOS 服务端加密。
- 开启版本控制和生命周期；删除必须清除全部版本与删除标记。
- 用户可删除内容不得使用 WORM/Object Lock。

本次仅修订文档，没有初始化 Git、开发代码、连接 Devbox、创建 TOS 桶或产生费用。

### 4.22 2026-08-18：Git 基线与双远端建立

用户提供 GitHub 和内部代码仓库地址，并明确授权将当前项目初始化为 Git 仓库后推送到两处。

本次完成：

- 在当前 Mac 工作区初始化 `main` 分支。
- 将四份基线文档作为首个提交。
- 配置 GitHub `origin` 作为上游与分支跟踪远端。
- 配置内部 `bits` 作为同提交镜像。
- 验证两个远端的 `main` 指向同一提交，且本地工作区干净。

本次没有创建协议、Core、客户端或基础设施代码，没有连接 Devbox、创建 TOS 资源或产生费用。文字模型和 TOS 不阻塞协议、目录与确定性测试适配器开发，但分别在真实对话闭环和备份切片开始前成为必需依赖。

## 5. 已锁定的关键结论

- Violet 是持续存在的私人智能体，不是聊天框、工具集合或单一 Agent。
- 当前只服务用户本人。
- 用户只面对一个 Violet。
- Violet 与用户亦敌亦友，不盲从，也不自封为监护人。
- 用户拥有治理权，但不能强迫 Violet 得出指定结论。
- 未唤醒时不持续感知；第一阶段不主动发起新互动。
- 感知由用户主动开启，设备端是数据出境守门人。
- Mac 全局感知是基础，应用适配器只增强。
- 统一记忆持续存在，不存在产品层面的新建会话。
- Violet 自主写记忆，用户完整查看、编辑和删除。
- 记忆准确性高于表面连续性。
- 删除必须传播到在线内容、索引、缓存、任务、对象版本和备份恢复路径，并可验证不可恢复。
- 自然表达是唯一入口，Violet 自行选择聊天、回答或行动。
- 认知自由，行动受托。
- 委托范围内自主执行，高风险事前审批。
- Violet 对交付质量负责，用户最终验收。
- 低风险进化可静默但可审计，高风险进化必须事前同意。
- 自我进化不作为第一版基础开发能力的前提。
- 开发能力建设通用基础设施，不绑定产品类型。
- 简单任务单 Agent，复杂任务动态组队。
- 所有具备文件、终端、Git 或浏览器操作能力的工具必须运行在最小隔离 Worker，不能进入 Core。
- 前后端严格分离，模块化单体起步。
- Devbox 是当前临时云环境，必须可迁移。
- Git 提交历史是唯一代码事实源；GitHub `origin` 是上游，内部 `bits` 保存同提交镜像，Devbox 只部署已同步的确定提交。
- `1A` 使用 Mac `dev-cli`、SSH 隧道、sealed Core 和应用层加密。
- TOS 是对象存储，OpenTelemetry + Grafana LGTM 是可观测基线。
- Violet 可以只读查看脱敏运行证据进行自我诊断，不能篡改审计。
- “为人民服务”是最高使命。

## 6. 关键矛盾及解决结果

| 关键矛盾 | 最终解决 |
|---|---|
| 超级工具还是智能个体 | 智能个体；工具能力只是表达方式 |
| 四项能力是否选择一个优先 | 都是成立条件，第一版范围小但人格完整 |
| 服从当前用户还是守护长期用户 | 照亮冲突，不替用户裁决人生 |
| 随时在场是否意味着持续监控 | 随时可达，感知必须主动开启 |
| 自主记忆是否侵犯用户主权 | Violet 自主写，用户完整知情、编辑和删除 |
| 用户完整管理是否等于可操纵结论 | 管理存在和演化，不能强迫指定结论 |
| 独立思考是否意味着自由行动 | 认知自由，行动受托 |
| 像朋友是否需要主动联系 | 第一阶段不主动，新互动由用户发起 |
| 原始数据不长期保存是否会失忆 | 原始感知短暂，关系、任务和提炼记忆连续 |
| 继续旧对话还是新建会话 | 不做会话选择；统一记忆、动态注意力 |
| 模型自行判断是否足够安全 | 模型判断意图，确定性系统管权限、状态和证据 |
| 交付由 Violet 判断还是用户判断 | Violet 判断可交付，用户最终验收 |
| 静默进化是否意味着不可见 | 不打扰但可审计；高风险事前审批 |
| 先固定一种产品是否便于验证 | 不固定产品，以通用基础设施和多样任务评估 |
| 是否按前端到企业级规划能力 | 不按品类限制，按风险、工具和证据动态决定 |
| 多 Agent 是否天然更强 | 不是；只有协调收益超过成本时组队 |
| 浏览器扩展是否是 Violet 的眼睛 | 只是增强；Mac 全局感知才是基础 |
| 世界级架构是否需要立即微服务 | 不需要；契约和边界优先，模块化单体起步 |
| 公司 Devbox 是否可作云环境 | 当前可以作临时云，但必须最小数据、可迁移 |
| 真实记忆是否可进入 Devbox | 用户已确认合规；仍禁止绝对秘密和未授权敏感原文 |
| 删除后备份是否会让记忆复活 | 新备份后销毁旧备份代密钥，恢复前重放删除墓碑 |
| Violet 能否看自己的日志 | 可以只读查看脱敏运行证据，不得篡改或关闭观测 |
| 对象存储选择什么 | 火山引擎 TOS，通过 S3 Port 接入，私有桶和信封加密 |

## 7. 当前尚未锁定的技术决策

以下问题不得由后续 Agent 静默假设：

- 正式文字模型和多模态模型供应商。
- 本地唤醒词引擎。
- 任务运行时是否最终采用 LangGraph.js。
- 代码 Agent 是否采用 OpenHands 或其他引擎。
- 沙箱采用 Devbox 容器、独立远程沙箱或混合方式。
- TOS 地域、桶名称、IAM 子账号和服务端加密方式。
- 正式个人云供应商和迁移时间。
- Safari、Firefox 等应用增强适配器的具体交付顺序。

决策方式必须是：定义验收标准、做最小真实测试、记录证据和取舍，再写入架构文档。

## 8. 当前进度

```text
产品愿景与宪法              完成
总体架构方向                完成
第一阶段与长期工程路线      完成
持续历史记录                已建立
Sprinkle 只读评估           完成
Devbox 只读环境确认         完成
Git 仓库与双远端基线        完成
协议与目录骨架              未开始
Devbox 基础服务部署         未开始
Mac App                     未开始
统一记忆                    未开始
持久任务运行时              未开始
开发交付 Worker             未开始
第一阶段出生验收            未开始
```

项目当前处于：**Git 基线已建立，尚未批准进入协议、Core 或基础设施实现。**

## 9. 下一步计划

按照 [工程路线](./engineering-roadmap.md) 执行第一阶段。

最近一步是 Release 1A：Violet Seed。

执行前需要用户明确说“开始实现”“Implement the plan”或同等含义。获得批准后：

1. 配置主分支保护规则。
2. 建立单仓库目录与工程约束。
3. 建立 `apps/dev-cli`、JSON Schema 事件事实源、OpenAPI 和生成 SDK。
4. 建立 SSH 隧道、设备令牌、密钥注入和 sealed Core。
5. 建立 Devbox 可迁移 Docker Compose 基座。
6. 建立模块化 Core、PostgreSQL、模型 Port 和应用层加密。
7. 接入 TOS 加密备份及 OpenTelemetry / Grafana LGTM。
8. 完成文字对话、身份和事件持久化纵向闭环。
9. 通过认证、密钥扫描、空环境部署、备份和恢复验证。

第一阶段后续依次交付：

```text
1A Violet Seed
→ 1B Violet Presence
→ 1C Violet Sight
→ 1D Violet Continuity
→ 1E Violet Agency
→ 1F Violet Delivery
→ 1G 浏览器增强与出生验收
```

## 10. 新 Agent 安全交接规则

任何新接手的模型或工程师必须遵守：

- 先读本文和三份基线文档，再读当前代码和 Git 状态。
- 不把记忆或旧会话当作当前操作授权。
- 未经用户批准，不开始实现、部署、安装、付费、发布、删除或扩大权限。
- 不读取、上传或处理保密工作内容。
- 不在 Devbox 保存绝对秘密、受控敏感原文和个人生产密钥。
- 不将 Devbox IP、公司域名、公司鉴权或内部服务写入领域核心。
- 不把 Sprinkle 扩建成 Violet 本体。
- 不把浏览器或某种产品类型作为 Violet 的能力边界。
- 不让 Subagent 默认获得完整个人记忆。
- 不允许开发 Agent 自行批准自己的交付。
- 不允许 Core 直接执行文件、终端、Git、Playwright 或其他不可信工具。
- 不用模型自信替代测试、证据和回滚。
- 不静默修改产品宪法、权限、安全或审计规则。
- 不让 Violet 获得删除、篡改、关闭或扩大观测系统的权限。
- 不因追求“世界级”而提前引入无必要的微服务、队列或平台复杂度。
- 代码和运行证据只能说明当前实现。与宪法或已批准设计冲突的代码必须视为缺陷并停止交付，不能反向覆盖文档。
- 发现新风险、失败模式或推翻旧决策时，必须更新本文。

## 11. 历史记录维护规则

每次更新至少维护：

- 最近更新时间。
- 当前状态和进度。
- 新决策及其证据。
- 被推翻或修正的旧结论，以及修正原因。
- 新风险和未决问题。
- 下一步计划。
- 已完成阶段的验收结果和回滚点。

不要删除旧时间线。决策变化时使用“已被某日期的决策取代”标记，让后续接手者理解为什么改变。

禁止写入：

- 密码、验证码、Token、私钥和真实密钥。
- 无必要的身份证、医疗、财务或他人隐私原文。
- 可由 Git、数据库或监控系统查询的冗长原始日志。
- 未脱敏的公司保密信息。

## 12. 当前结果

本轮产品发现和架构讨论已经将最初的“像贾维斯一样的东西”收敛为：

> 一个以用户为中心、跨设备连续、主动感知受控、统一记忆、认知自由、行动受托、结果负责、可治理进化，并以为人民服务为最高使命的可自我进化的私人智能体。

已经从愿景推进到可执行工程路线，但尚未产生 Violet 可运行代码。下一次关键跃迁不是继续增加理念，而是获得用户批准后完成 Release 1A 的第一个可运行纵向闭环。
