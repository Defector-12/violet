# 开发规则审计与处理记录

> 2026-09-07。本文记录规则审计结果和处理边界，不是 Agent 的规则文件。

## 已处理

- 新增根目录 `AGENTS.md`，作为唯一的项目级长期规则入口。
- `AGENTS.md` 只记录自主执行、权限、安全、隐私、变更范围和验证原则，不记录 Release
  状态、模型版本、部署哈希、测试数量或临时故障。
- 删除 2026-09-03 整包引入的 25 个通用项目 Skill。它们共 7,644 行，与平台已有能力
  重复，触发范围过宽，并包含不适用于 Violet 的 Web、npm、GitHub Actions 和团队发布
  假设。
- 删除随上述通用 Skill 生成但未纳入版本控制的 `skills-lock.json`。
- 一并消除了 19 处指向 7 个不存在的 `.trae/references/*.md` 文件的引用，以及对
  `AskUserQuestion`、`/build`、`/review`、`/loop` 和 `agents/` 等当前环境不存在入口
  的依赖。
- `architecture-direction.md` 不再维护动态发布流水账；当前实现、部署、测试和门禁由
  对应验收文档统一管理。

## 全局配置（2026-09-07）

- 未直接修改产品管理的内置 Skill 文件；通过 `~/.trae-cn/skill-config.json` 禁用
  `TRAE-debugger`，避免其强制 HTTP 插桩、重复用户复现和确认后清理流程自动触发。
- 同样禁用与 `bits-code-guard` 重叠的 `TRAE-code-review`，代码审查只保留一个默认入口。
- 用户安装的 `ponytail` 已改为仅在用户明确调用时启用，并取消跨请求常驻、固定输出长度
  和限制测试框架的规则。
- 用户安装的 `karpathy-guidelines` 已改为仅在用户明确要求额外谨慎审查时启用，不再与
  项目长期原则重复触发。

## Node 工具链

- 2026-09-08 确认仓库的 `.node-version` 已固定为 22.23.2，`package.json` 也声明了
  Node.js 与 pnpm 版本范围；缺失的不是版本声明，而是非交互 Shell 的版本激活。
- Agent 命令进程继承了 fnm 默认 Node.js 18.20.8。`fnm --use-on-cd` 依赖 Shell 初始化
  或后续目录切换，无法覆盖直接以仓库为工作目录启动的非交互进程；单次执行 `fnm use`
  也不会影响下一条 Agent 命令，因此此前的临时修复会重复失效。
- 已将本机 fnm 默认版本切换到仓库已安装的 22.23.2，供新启动的进程使用；仓库同时在
  `AGENTS.md` 要求本地 Agent 命令通过
  `fnm exec --using=.node-version -- <command>` 执行，避免依赖父进程 PATH、登录 Shell
  或 `cd` 钩子。

## 未修改

- TRAE 为当前工作区生成的 Code Mode、Computer Use、Context7 和 Record & Replay
  元数据不是仓库配置，不手工修改生成文件。仓库当前没有 `.mcp.json`。
- 运行时视觉 Prompt 暂不在规则清理中改动。当前工作区存在进行中的视觉定位诊断代码；
  Prompt 变化会改变模型行为，必须作为独立实验，用固定样本和真实多次回放验证。

## 后续规则

- 只有出现稳定、重复且项目特有的工作流时，才新增 Violet Skill；单次经验写入文档或
  测试，不为其创建 Skill。
- 新 Skill 必须有窄触发条件、真实可用的工具、完整引用和退出条件，不得要求自动提交、
  安装依赖、修改全局配置或把可自动完成的复现交还用户。
- MCP 按任务启用并保持最小权限。项目若需要固定 MCP，应使用仓库级配置并锁定版本，
  不使用运行时下载的 `@latest`。
- 确定性安全边界保留在代码中；模型 Prompt 负责表达目标和证据语义，不替代权限、
  隐私、输入校验或发布门禁。

## 保留原则

保留隐私过滤、绝对秘密阻断、明确授权、当前轮次绑定、失败关闭、不损坏用户工作、
真实证据、统计评估和用户最终验收权。规则收敛降低的是流程噪声，不是质量或安全标准。

## 2026-09-13 经用户批准的优化

依据 OpenAI 的
[Skills 与提示词审计文章](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)
完成只读审计，用户随后批准执行全部八项最小修改：

- 保留 `AGENTS.md` 原样，不新增覆盖性规则；自主执行、工具链、安全和测试留痕要求不变。
- 全局技能配置停用 `bits-unit-test-gen` 和重复的 `dynamic-ui`，保留
  `TRAE-dynamic-ui`。普通单测使用仓库测试与记录入口；需要 BITS 时显式重新启用。
- `think` 删除批准后再等一次实施指令，以及按修改文件数强制暂停修复的冲突条款。
- 测试规范区分端到端链路录制与纯本地命令记录，不让本地单测等待 Mac/Core。
  每次测试仍有独立记录，缺失证据不能冒充 N/A。
- `bits-code-guard` 主文件与工作流参考统一为文字结论默认、报告文件按需生成；
  不再默认追问是否打开浏览器。
- `read` 主文件和参考统一走已有的本地优先抓取脚本；不默认安装解析器、CLI 或
  浏览器，第三方代理需要授权且不得接收敏感 URL，飞书内容交给已有连接器。
- 架构第 18、19 节的动态证据和过期待办迁至 Release 1C 验收的历史背景，
  不新增验收通过项，不重新开启已完成的终端修复，不改变发布门禁。
- Context7 复用现有 Node 22.23.2 的绝对 `npx` 路径与 PATH；Chrome MCP 从
  `@latest` 固定到本机缓存的 `1.9.0`，保留 `--slim`、`--headless`。
  bytedcli 配置、权限和自动升级禁用设置不变。

本次只改文档、技能文本和配置，不修改产品代码、模型 Prompt，不安装依赖、部署、
提交或推送。全局修改影响其他工作区；当前会话已加载的技能描述与运行中 MCP 不保证
热更新，应以之后新会话/服务重连实际加载结果为准，不主动中断当前 IDE 会话。

`think`、`read`、`bits-code-guard` 是本次获批的内置文件本地补丁，产品更新可能覆盖；
没有新建同名覆盖 Skill，也没有修改 MCP 生成元数据。修改前文件含原有未提交改动，
已备份到 `.local-acceptance/rules-optimization-20260913-jHG3Vl/before.tar.gz`。
回退时按该备份逐文件比较并恢复本次差异，不整包覆盖后续用户改动。

定向验证 12 项通过：包含 `jq` 语法、技能/MCP 精确配置差异、`AGENTS.md` 未改变、
既有验收证据完整保留、文档链接、空白检查及缓存 Chrome MCP 的版本和参数支持。
运行记录为 `.local-acceptance/test-runs/d9d9f2e2-6989-45d6-a146-f58faae0e7d0/REPORT.md`。
未测试 IDE 热加载、MCP 握手或模型行为改善，未运行产品全量回归。本段为验证后的结果
归档，不在该次文件哈希快照中；不能把配置检查通过等同于实际延迟或模型效果已验证。
